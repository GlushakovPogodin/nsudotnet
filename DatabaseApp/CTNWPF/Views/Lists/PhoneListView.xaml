<UserControl x:Class="CTNWPF.Views.Lists.PhoneListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox ItemsSource="{Binding PhoneList}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ATSName}"/>
                        <TextBlock Text="{Binding AddressStr}"/>
                        <TextBlock Text="{Binding Type}"/>
                        <TextBlock Text="{Binding PhoneNumber}"/>
                        <TextBlock Text="{Binding SubscriberSurname}"/>
                        <TextBlock Text="{Binding SubscriberName}"/>
                        <TextBlock Text="{Binding SubscriberPatronymic}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
